# Optional. Without this, the app uses the keyless DuckDuckGo HTML endpoint.
# Get a free key at https://serper.dev (Google SERP, much higher hit-rate).
# Copy this file to .env.local and fill in the value, then restart `npm run dev`.
SERPER_API_KEY=
